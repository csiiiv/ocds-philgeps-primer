import { FLAGSHIP_RELEASES, type WorkedRelease } from "./workedExample";

/** Releases come from the same example object that feeds every other station. */
export const DEFAULT_RELEASES: WorkedRelease[] = FLAGSHIP_RELEASES;
